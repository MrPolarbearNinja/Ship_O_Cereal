/*
function set_res () {
    var resHeight = (window.screen.height - 80).toString();
    document.getElementById("main-container").style.minHeight = resHeight + "px";
    console.log(resHeight + "is " + typeof(resHeight));
    console.log(window.screen.width)
}

set_res()*/
